import 'package:flutter/material.dart';

class Trip_Details extends StatelessWidget {
  const Trip_Details({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
