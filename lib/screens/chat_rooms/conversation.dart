import 'package:flutter/material.dart';

class ConverstaionRoom extends StatelessWidget {
  const ConverstaionRoom({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(children: []));
  }
}
